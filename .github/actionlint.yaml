# actionlint config — declare the self-hosted runner's custom labels so actionlint
# (run in CI) doesn't flag `runs-on: [self-hosted, linux, x64, parking]` as unknown.
self-hosted-runner:
  labels:
    - parking
