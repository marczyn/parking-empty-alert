# ⚠️ TEMPLATE — these are placeholder values committed to git.
# After running `bash scripts/setup.sh` these will be overwritten with your real
# credentials. Do NOT git commit your local copy after running setup.

# ─── MQTT (internal broker authentication) ───
mqtt_user: frigate
mqtt_password: change_this_password_too

# ─── WhatsApp via CallMeBot ───
# Phone number in international format WITHOUT "+", "00", spaces, dashes
# Example Poland: 48501234567
whatsapp_phone: "48501234567"
# APIKEY from CallMeBot response (after sending "I allow callmebot to send me messages")
whatsapp_apikey: "1234567"
